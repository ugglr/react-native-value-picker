import React from "react";
import { View, Text, TouchableOpacity, FlatList } from "react-native";

function ScrollPicker(props) {
  return (
    <View>
      <Text>React Native ScrollPicker</Text>
    </View>
  );
}

export default ScrollPicker;
